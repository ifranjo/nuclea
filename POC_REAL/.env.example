# =============================================================================
# POC_REAL — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values.
# For local Supabase development, run `npx supabase start` to get the keys.

# -----------------------------------------------------------------------------
# Supabase (required)
# -----------------------------------------------------------------------------

# Public Supabase URL — used by client and server components
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321

# Public anonymous key — used by browser client (safe to expose)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key — server-only, full DB access (NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------

# Public-facing app URL — used in emails, share links, and OG metadata
NEXT_PUBLIC_APP_URL=http://localhost:3002

# -----------------------------------------------------------------------------
# Beta Gate
# -----------------------------------------------------------------------------

# Enable beta access gate in middleware (true|false)
# When OFF (default), all authenticated users can access protected routes.
# When ON, middleware checks beta_access table before allowing access.
BETA_GATE_ENABLED=false

# Admin API secret — required for admin-only endpoints (POST /api/beta/invite, etc.)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_SECRET=

# -----------------------------------------------------------------------------
# Cron Jobs
# -----------------------------------------------------------------------------

# Shared secret for authenticating cron requests (x-cron-secret header or Bearer token)
CRON_SECRET=

# Hours before capsule expiry to notify trust contacts (default: 72)
TRUST_CONTACT_NOTIFY_BEFORE_HOURS=72

# -----------------------------------------------------------------------------
# Seed Script (optional)
# -----------------------------------------------------------------------------

# Alternative URL for seed script (defaults to NEXT_PUBLIC_SUPABASE_URL)
# SUPABASE_URL=http://127.0.0.1:54321

# Custom directory for seed images (defaults to public/images/polaroids)
# SEED_IMAGES_DIR=/path/to/images
